@model IEnumerable<FirstMVCproject.Models.AccountDetail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Aname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aaddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Abalance)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Aname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aaddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Abalance)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Anumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Anumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Anumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
